global:
  tlsDisable: false
injector:
  image:
    pullPolicy: Never
  affinity: null
server:
  dev:
    enabled: true
  logLevel: debug
  extraArgs: "-dev-tls -dev-tls-cert-dir=/tmp"
  extraEnvironmentVars:
    VAULT_CACERT: /tmp/vault-ca.pem
